import MainHeaderBackground from "./components/main/main-header-background";
import "./globals.css";
import MainHeader from "./components/main/main-headers";

export const metadata = {
  title: "NextLevel Food",
  description: "Delicious meals, shared by a food-loving community.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>
        <MainHeader />
        {children}
      </body>
    </html>
  );
}
